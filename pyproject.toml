[project]
requires-python = ">=3.11,<3.13" # blocked by imap_processing https://github.com/IMAP-Science-Operations-Center/imap_processing/blob/dev/pyproject.toml#L17
name = "imap-mag"
version = "6.7.2"
description = "Process IMAP data"
authors = [
  {name = "alastairtree"},
  {name = "mhairifin"},
  {name = "mfacchinelli"},
  {name = "jean-imperial"},
  {name = "miruuna"},
  {name = "dalonsoa"},
]
readme = "README.md"
dependencies = [
    "pyyaml>=6.0.3",
    "typing-extensions>=4.15.0",
    "pydantic>=2.13.5",
    "space-packet-parser>=6.1.0",
    "xarray>=2025.6.1",
    "numpy>=2.4.6",
    "typer>=0.16.0,<0.21.0",
    "sqlalchemy>=2.0.51",
    "alembic>=1.19.1",
    "sqlalchemy-utils>=0.42.0",
    "requests>=2.34.2",
    "pandas>=3.0.3",
    "imap-data-access (==1.0.0)",
    "cdflib>=1.3.12",
    "psycopg[binary]>=3.3.4",
    "prefect[shell]==3.7.8",
    "prefect-docker>=0.7.3",
    "spacepy>=0.7.0",
    "pydantic-settings>=2.15.0",
    "ccsdspy>=2.0.0",
    "crump>=0.7.1",
    "prefect-managedfiletransfer>=0.6.2",
    "ialirt-data-access>=0.6.0",
    "apprise>=1.13.1",
    "spiceypy>=8.2.0",
    "prefect-sqlalchemy>=0.6.1",
    "imap-processing>=1.0.34",
    "prefect-github (>=0.4.3,<0.4.4)",
]


[tool.poetry]
packages = [
    { include = "imap_mag", from = "src" },
    { include = "imap_db", from = "src" },
    { include = "mag_toolkit", from = "src" },
    { include = "prefect_server", from = "src" },
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.14.1",
    "pytest-env>=1.7.0",
    "pytest-asyncio>=1.4.0",
    "pre-commit>=4.5.1",
    "ruff (>=0.16.0,<0.17.0)",
    "docker>=7.1.0",
    "testcontainers>=4.14.1",
    "wiremock>=2.7.0",
    "pytest-xdist>=3.8.0",
    "diffimg>=0.3.0",
    "openpyxl>=3.1.5",
]

[tool.poetry.scripts]
# can execute via poetry, e.g. `poetry run imap-mag hello world`
imap-mag = 'imap_mag.main:app'
imap-db = 'imap_db.main:app'

[tool.pytest.ini_options]
pythonpath = [
  ".", "src"
]
minversion = "8.0"
testpaths = [
    "tests/*.py",
    "tests/**/*.py",
]
env = [
    "PREFECT_LOGGING_EXTRA_LOGGERS=imap_mag,imap_db,mag_toolkit,prefect_server,imap_data_access,ialirt_data_access",
    "PREFECT_LOGGING_LEVEL=INFO",
    # Don't ship every flow/task log to the ephemeral Prefect API during tests - it
    # adds significant per-flow-run overhead. Logs are still captured via Python
    # logging (e.g. the capture_cli_logs fixture), so log assertions keep working.
    "PREFECT_LOGGING_TO_API_ENABLED=false",
    "MPLBACKEND=Agg"
]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
# Exclude a variety of commonly ignored directories.
exclude = [
    "tests/datastore",
    "docs",
    "output",
    "data",
    "htmlcov",
    "src/IMAP_MAG_Calibration",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle
    "F",   # Pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "RUF"  # ruff
]
pydocstyle.convention = "google"

[tool.ruff.lint.per-file-ignores]
# Ignore:
#  * Line too long
#  * Optional replaced as X | None
"*" = ["E501", "UP007"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
